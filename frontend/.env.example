# Publiek (wordt bij Next build ingevoegd in de bundle). Wijziging vereist **nieuwe Vercel deploy**.
NEXT_PUBLIC_API_URL=https://futures-production-3c71.up.railway.app

# Server-only (aanbevolen op Vercel): wordt bij **elke request** gelezen — geen rebuild nodig na wijziging.
# Zet dezelfde Railway-URL als hierboven (zonder trailing slash).
DASHBOARD_API_URL=https://futures-production-3c71.up.railway.app
